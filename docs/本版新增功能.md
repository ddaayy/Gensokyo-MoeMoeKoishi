# 本版新增功能

本 Fork（Te-River/Gensokyo-NewQQ）基于 [Hoshinonyaruko/Gensokyo](https://github.com/Hoshinonyaruko/Gensokyo) 修改，以下为所有扩展内容。

## 新增事件

| 事件 | 说明 | 对应 Intent | 适用范围 |
|------|------|-------------|:--------:|
| `GroupMessageEventHandler` | **非@群消息**接收。原版只支持 `GROUP_AT_MESSAGE_CREATE`（@消息），新增 `GROUP_MESSAGE_CREATE` 支持 | `GroupMessageEventHandler` | 🏷️ 群聊 |
| `MemberEventHandler` | **频道成员变动**通知。`GUILD_MEMBER_ADD` → `notice.group_increase`（`sub_type=member`），`GUILD_MEMBER_REMOVE` → `notice.group_decrease`（`sub_type=member`） | `MemberEventHandler` | 📡 频道 |
| `GroupMemberAddEventHandler` | **群成员新增**通知（QQ API 未文档化事件）。→ `notice.group_increase`（`sub_type=member`） | `GroupMemberAddEventHandler` | 🏷️ 群聊 |
| `GroupMemberRemoveEventHandler` | **群成员移除**通知（QQ API 未文档化事件）。→ `notice.group_decrease`（`sub_type=member`） | `GroupMemberRemoveEventHandler` | 🏷️ 群聊 |

## 新增 API

| Action | 说明 | 文件 | 适用范围 |
|--------|------|------|:--------:|
| `get_avatar` | 获取用户 QQ 头像直链（[文档](./api/扩展api/扩展api-get_avatar.md)） | get_avatar.go | 🌐 全场景 |
| [`get_robot_share_link`](./api/扩展api/扩展api-get_robot_share_link.md) | 获取机器人分享链接 | get_robot_share_link.go | 🌐 全场景 |
| [`send_private_msg_wakeup`](./api/扩展api/扩展api-send_private_msg_wakeup.md) | 发送被动唤醒私聊消息 | send_private_msg_wakeup.go | 💬 C2C 私聊 |
| [`put_interaction`](./api/扩展api/扩展api-put_interaction.md) | 处理按钮交互回调 | put_interaction.go | 🌐 全场景 |
| `set_group_card` | OneBot 11 Mock 兼容群名片设置（防崩溃） | set_group_card.go | 🌐 全场景 |
| `set_group_add_request` | OneBot 11 Mock 兼容加群申请处理（防崩溃） | set_group_add_request.go | 🌐 全场景 |
| `/metrics` | 原生 Prometheus 监控度量数据接口 | main.go | 🌐 全场景 |

> 图例：🌐 全场景 = 频道 + 群聊 + C2C 均可 | 🏷️ 群聊 | 📡 频道 | 💬 C2C 私聊

## 新增特性

### 群消息 `to_me` 字段

> **适用范围**：🏷️ 群聊

群消息事件新增 `to_me` 字段，标识消息是否 @ 了机器人。

```json
{
    "group_id": 123456,
    "message": [...],
    "to_me": true,
    "user_id": 789012
}
```

- `GROUP_AT_MESSAGE_CREATE` → `to_me: true` 始终为 true
- `GROUP_MESSAGE_CREATE` → 检测 `Mentions` 中是否有 `IsYou=true`

### 非@消息 @ 处理

> **适用范围**：🏷️ 群聊（仅 `GROUP_MESSAGE_CREATE`）

`GROUP_MESSAGE_CREATE` 事件中的 @ 处理。

| 场景 | 行为 |
|------|------|
| 入站 `@bot` | 从 content 中剥离，`to_me = true` |
| 入站 `@其他人` | 转为 `[CQ:at,qq=虚拟ID]`，建立双向映射 |

### 出站 @ 格式

> **适用范围**：🏷️ 群聊 + 📡 频道

| 场景 | 旧格式 | 新格式 |
|------|--------|--------|
| 纯文本出站 | `<@!OpenID>` | `<qqbot-at-user id="OpenID" />` |
| Markdown 出站 | 不支持 | `<qqbot-at-user id="OpenID" />`（自动转换 `[CQ:at]`）|

### Markdown 中的 @ 能力

> **适用范围**：🏷️ 群聊 + 📡 频道

在 Markdown 卡片内容中嵌入 `[CQ:at,qq=数字]`，Gensokyo 自动转换为 QQ API 的 `<qqbot-at-user>` 标签。

### Markdown 图片自动上传

> **适用范围**：🏷️ 群聊 + 📡 频道

Markdown 内容中的 `![](path)` 图片，HTTP(S) URL 直接保留，本地文件路径自动读取并上传到 QQ CDN 替换为直链。

### 按钮虚拟 ID 自动转化

> **适用范围**：🌐 全场景

遍历 keyboard 中所有按钮的 `specify_user_ids`，将虚拟数字 ID 自动转为真实 QQ OpenID，确保 QQ API 能正确识别权限用户。

### C2C 按钮 permission 修复

> **适用范围**：💬 C2C 私聊

C2C 单聊场景中，QQ 不支持 `permission.type=0`（指定用户），检测到 `specify_user_ids` 有值且 `type=0` 时强制改为 `type=2`（所有人）。

### action.type / permission.type 零值不被删除

> **适用范围**：🌐 全场景

修复 JSON `omitempty` 导致 `action.type=0`、`permission.type=0`、`render_data.style=0` 被序列化丢弃的问题。

### 语音编码优化（免 ffmpeg）

> **适用范围**：🌐 全场景（群聊 + 频道 + C2C 均支持语音发送）

支持 `[CQ:record]` 语音消息的自动转码（编码为 SILK v3），无需安装 ffmpeg。

| 格式 | 解码方式 | 说明 |
|------|----------|------|
| **WAV** (RIFF/WAVE) | 直接提取 PCM | 16-bit 单声道、匹配采样率时直接提取 |
| **MP3** | Go 解码 | 使用 `go-mp3` 库，无外部依赖 |
| 其他 (OGG/FLAC/AAC 等) | 回退 ffmpeg | 安装 ffmpeg 后可支持 |

> 语音文件必须符合 **16-bit、单声道、采样率 24000**（可在配置中调整 `record_sampleRate`）。不满足时会自动尝试通过 ffmpeg 转换。

### [CQ:reply] → message_reference 引用回复

`[CQ:reply,id=数字]` 不再被丢弃或作为纯文本发送，改为：

- 自动从文本中剥离
- 若同时存在 MD 卡片，将 `message_reference` 挂载到 MD 消息上合并发送

> ⚠️ **重要限制：**
> - 引用回复**仅在频道中**可正常显示引用样式（`GROUP_AT_MESSAGE_CREATE`）
> - **群聊中**（`GROUP_MESSAGE_CREATE`）QQ API **接受但不会渲染**引用样式——API 返回 200，但用户看不到引用效果
> - **Markdown 消息** QQ API **不支持** `message_reference` 参数，引用字段会被静默忽略

**`message_reference` 字段结构：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `message_id` | string | 被引用的消息 ID（自动从 `[CQ:reply,id=...]` 解析） |
| `ignore_get_message_error` | bool | 忽略获取消息时的错误，默认为 `true` |

### 数据库多 Bot 隔离（新增配置）

多个 Bot 共享同一 `idmap.db` 时，通过 UIN 前缀隔离各自的映射空间：

```yaml
idmap_isolation: false      # 数据库 key 加 UIN 前缀，不同 Bot 互不干扰
idmap_legacy_compat: false  # 同时写入旧格式 key，兼容官方 Gensokyo
```

**三种模式：**

| idmap_isolation | idmap_legacy_compat | 正向 key | 反向 key |
|:---:|:---:|---------|---------|
| `false` | 忽略 | `"OpenID"` | `"row-ID"` |
| `true` | `false` | `"UIN:OpenID"` | `"row-UIN:ID"` |
| `true` | `true` | 两者都写 | 只写新格式 |

> 启用 `idmap_isolation` 后，之前的绑定 API（`type=5`）行为改变：不再是迁移虚拟 ID，而是新增一个 `"UIN:真实QQ"` 的前向映射（alias），避免目标虚拟 ID 已被占用时的冲突。

### idmap 三库分离 + 全量迁移 + msg_id 自动过期

> **适用范围**：🌐 全场景

将原来单 `idmap.db` 拆分为三个独立数据库，迁移范围 3 个桶（跳过 cache）：

| 文件 | 桶 | 用途 | 特性 |
|------|-----|------|------|
| `idmap-identity.db` | `ids` | OpenID ↔ 虚拟数字 ID | 永久数据 |
| `idmap-identity.db` | `config` | 运行时配置 | 永久数据 |
| `idmap-identity.db` | `UserInfo` | 用户信息缓存 | 永久数据 |
| `idmap-msg.db` | `cache` | 消息 ID 映射 | ⏱ **6 分钟自动过期清理** |
| `idmap.db`（旧） | — | 旧版单库 | 迁移源，数据全部迁出后可手动删除 |

**迁移流程（3 阶段）：**

1. **计数器同步**（阻塞）：仅同步 `ids` 桶 counter。`cache` 桶跳过迁移，新 msg DB 从零开始。
2. **后台迁移**（3 个桶）：`ids` → `config` → `UserInfo`（不迁移 `cache`），每批 100 条，10ms 间隔
3. **数据校验**：游标流式校验 3 个桶，跳过 cache

**msg_id 缓存自动过期：**
- 每次写入 msg_id 时同时写入时间戳 `ts:row-{虚拟ID}`
- 后台 goroutine **每分钟扫描** `idmap-msg.db`
- 存在时间 **≥ 6 分钟**的条目自动删除（前向 key + 反向 key + 时间戳）
- 旧库 `cache` 桶数据不迁移新库，避免积累数百万条无效映射

**迁移完成后自动切换：**
- `StoreIDv2` → `storeIdentity`（纯新库，不再写旧库）
- `StoreCachev2` → `StoreMsgID`（纯新库，不再写旧库，带 6min 过期）
- `WriteConfig`/`ReadConfig`/`StoreUserInfo`/`ListAllUsers` 路由至 identityDB

### Sender.Nickname 自动填充

自动使用 `Author.Username` 填充 `event.sender.nickname`：

```json
{
    "sender": {
        "nickname": "用户名",   // 从 Author.Username 获取
        "user_id": 123456789
    }
}
```

优先顺序：`card_nick` 配置 > `Author.Username` > 空

### 群成员变动通知

> **适用范围**：🏷️ 群聊

`GROUP_MEMBER_ADD` / `GROUP_MEMBER_REMOVE` 事件支持（独立 Intent `1<<24`，需在 `text_intent` 中启用 `GroupMemberAddEventHandler` 和 `GroupMemberRemoveEventHandler`）。

Gensokyo 捕获事件后以后端插件推送 `message.group.normal` 事件，`message` 字段包含纯 CQ 码标记：

```
[message.group.normal]: [CQ:member,type=add,group_id=821404315,user_id=3607918353]
```

#### 双向流程

| 阶段 | 方向 | 说明 |
|:----:|:----:|------|
| Gsk → 后端 | 入站 | `message.group.normal` 事件，`message` 为 `[CQ:member,type=add,group_id=821404315,user_id=3607918353]` |
| 后端 → Gsk | 出站 | 后端用普通 `send_group_msg` 回复，消息中包含 `[CQ:member]` + 文本。Gsk 自动解析 CQ 码、转换虚拟 ID、处理被动回复/主动推送 |

详见 [cq-member.md](./cq码/扩展CQ码/扩展cq码-cq-member.md) 文档。

#### nonebot2 示例

```python
from nonebot.adapters.onebot.v11 import GroupIncreaseNoticeEvent, GroupDecreaseNoticeEvent

@on_notice().handle()
async def handle_group_increase(bot: Bot, event: GroupIncreaseNoticeEvent):
    # 读取 message 字段中的 CQ 码
    cq_msg = event.message  # "[CQ:member,type=add,user_id=3607918353]"
    await bot.send_group_msg(
        group_id=event.group_id,
        message=f"{cq_msg}欢迎新成员！"
    )

@on_notice().handle()
async def handle_group_decrease(bot: Bot, event: GroupDecreaseNoticeEvent):
    cq_msg = event.message
    await bot.send_group_msg(
        group_id=event.group_id,
        message=f"{cq_msg}离开了我们"
    )
```

### 频道成员变动通知

> **适用范围**：📡 频道

`GUILD_MEMBERS (1 << 1)` Intent 支持，接收频道成员加入/离开事件并转为 OneBot 标准 notice。

| QQ API 事件 | OneBot 通知类型 | sub_type |
|:-----------:|:---------------:|:--------:|
| `GUILD_MEMBER_ADD` | `notice.group_increase` | `member` |
| `GUILD_MEMBER_REMOVE` | `notice.group_decrease` | `member` |

自动建立新成员的 OpenID 与虚拟 ID 映射。需在 `text_intent` 中取消 `MemberEventHandler` 注释启用。

### 群消息 `sender.role` 支持 `member_role`

> **适用范围**：🏷️ 群聊

群消息 `sender` 中的 `role` 字段现在会优先使用 QQ API 返回的真实身份标识，而非仅依赖配置文件中的 `master_id`。

#### 字段映射

| `member_role` 值 | `sender.role` 输出 | 含义 |
|:-----------------:|:------------------:|:----:|
| `owner` | `owner` | 群主 |
| `admin` | `admin` | 管理员 |
| `member` | `member` | 普通群员 |

#### 上报示例

群主发消息时，`sender` 如下：

```json
{
    "sender": {
        "user_id": 10000001,
        "role": "owner",
        "nickname": "群主"
    }
}
```

#### 优先级
1. QQ API 返回的 `member_role`（`owner` / `admin` / `member`）→ **优先使用**
2. 配置 `master_id` 列表匹配 → 设为 `owner`
3. 以上均不满足 → 设为 `member`

#### nonebot2 获取方式

```python
from nonebot.adapters.onebot.v11 import GroupMessageEvent

@matcher.handle()
async def _(event: GroupMessageEvent):
    role = event.sender.role  # "owner" / "admin" / "member"
    if role == "owner":
        await matcher.finish("群主你好")
```

### event_id 全场景存储

| 事件 | event_id 来源 |
|------|---------------|
| `INTERACTION_CREATE` | ProcessInlineSearch |
| `C2C_MSG_RECEIVE` | process_c2c_switch |
| `FRIEND_ADD` | process_friend |
| `GROUP_ADD_ROBOT` | ProcessGroupAddBot |
| `GROUP_MSG_RECEIVE` | ProcessGroupMsgReceive |

### 静态编译

CI 构建使用 `CGO_ENABLED=0`，兼容旧版 GLIBC。

### 高性能彩色分级日志与自动轮转归档 (`mylog`)

> **适用范围**：🌐 全场景

对程序内置日志进行了全面重構：
- **终端彩色高亮**：根据日志级别（`[DEBUG]` 蓝色、`[INFO]` 绿色、`[WARN]` 黄色、`[ERROR]` 红色、`[FATAL]` 粗红）为控制台提供清晰醒目的着色渲染，并支持配置 `log_color_enabled` 快速开关。
- **文件日志分级存储**：按天隔离目录，按级别分别存入 `logs/YYYY-MM-DD/info.log`、`warn.log`、`error.log` 等，且同时支持 Text 和 JSON 双结构化格式（由 `log_json_output` 开启）。
- **文件回滚机制**：单个日志文件写入达到上限后（默认 `100MB`，由 `log_max_size_mb` 决定）自动轮转重命名归档，防止日志过大打不开。
- **后台自动清理与压缩**：每日守护协程定时运作，自动用 `gzip` 压缩 7 天前的历史日志为 `.gz` 打包格式，并自动物理删除超过 30 天（由 `log_max_age_days` 决定）的过期旧日志。

### 原生 `/metrics` 监控与消息慢事件监测

> **适用范围**：🌐 全场景

- **监控指标暴露**：在主 Gin 上挂载 `/metrics` 端点，零外部三方依赖手动输出 Prometheus 标准文本规范。提供消息收/发总数、错误数、慢事件数等原子计数器指标。
- **消息慢事件检测**：核心处理入口内置基于 `defer` 耗时的超时拦截。单次事件执行若超出阈值（默认 `500ms`，由 `log_slow_event_threshold_ms` 决定），自增慢事件指标并输出 `[SLOW]` Warn 级别日志，便于排查因下游业务插件同步耗时或 IO 瓶颈带来的消息无响应问题。

### 管理员私聊快捷指令 (-status 与 -broadcast)

> **适用范围**：💬 C2C 私聊 (仅 Master 权限用户)

MasterID 列表中的管理员，向 Bot 发送私聊消息时可触发以下系统级管理指令（当 `master_id` 设为空数组时允许任何人触发）：
- `-status`：一键回显系统运行状态面板（包括 Uptime、当前分配内存、协程数、慢事件发生数及消息 IO 统计）。
- `-broadcast <内容>`：并发向 Bot 加入的所有频道和已知群聊批量发送通知。

### OneBot 11 API Mock 兼容垫片

> **适用范围**：🌐 全场景

拦截下游机器人框架（如 NoneBot、Mirai-Overflow）发送的特定敏感/高频不受官方支持的 API，打印 Mock 日志并快速回显 `{"status": "ok", "retcode": 0}`，从根本上解决下游框架因报错而意外崩溃的兼容问题：
- 修改群名片 (`set_group_card`)
- 处理加群请求 (`set_group_add_request`)

## 修复

- 按钮 `action.type`, `permission.type`, `render_data.style` 设置为 0 时被 JSON omitempty 删除
- 按钮中虚拟 ID 自动转化在私聊中失效（C2C permission.type 强制设为 2）
- `GetRemoveBotAtGroup` 误删用户主动发送的 @ 消息
- 入站 @bot 剥离逻辑使用 `Mentions[i].OpenID` 而非 `BotID`
- `ParseAndHandle` 添加错误日志和 panic 恢复
